import styled from 'styled-components';

export const div = styled.div`
   .icon_div {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;

      img {
         width: 100%;
         height: 100%;
         object-fit: contain;
      }
   }
`;
