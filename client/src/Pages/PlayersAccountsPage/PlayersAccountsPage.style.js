import styled from 'styled-components';

export const div = styled.div`
   .image_prev {
      width: 150px;
      height: 150px;
      border-radius: 5px;
      background-color: var(--main-cl);
      overflow: hidden;
   }
`;
