import styled from 'styled-components';

export const div = styled.div`
   padding: 1rem;
   img {
      width: auto;
      height: 30px;
   }

   .center_svg {
      position: absolute;
      left: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
   }
`;
