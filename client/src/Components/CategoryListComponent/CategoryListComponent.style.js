import styled from 'styled-components';

export const div = styled.div`
   width: 100%;
   border: 1px dashed var(--smooth-gray-cl);
   padding: 1rem;
   height: 750px;
   overflow-x: hidden;
   border-radius: 8px;
`;
