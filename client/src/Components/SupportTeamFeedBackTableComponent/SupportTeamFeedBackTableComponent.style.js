import styled from 'styled-components';

export const div = styled.div`
   .resolved {
      background-color: var(--light-green-cl);
      padding: 0.2rem 1rem;
      border-radius: 30px;

      p {
         color: var(--dark-cl);
      }
   }
`;
