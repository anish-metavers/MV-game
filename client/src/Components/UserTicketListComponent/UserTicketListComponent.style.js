import styled from 'styled-components';

export const tr = styled.tr`
   .user_div {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      overflow: hidden;

      img {
         transform: scale(1.3);
      }
   }
`;
